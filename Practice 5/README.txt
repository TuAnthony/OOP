In this implementation I had to do a hierarchical representation of the playing cards using class
inheritance. My card class also provided the ability to print the card, return the card color and also
was allowed to query if card is a standard card or joker.

